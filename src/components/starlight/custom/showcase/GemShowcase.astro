---
import fs from "node:fs";
import path from "node:path";
import GemCard from "./GemCard.astro";

const dataPath = path.resolve("public/data/wiki/gems.json");
const raw = fs.readFileSync(dataPath, "utf-8");
const data = JSON.parse(raw);
const gems = data.gems || [];
---

<div id="gem-showcase" class="not-content" data-gem-showcase>
    <div class="gem-filters">
        <div class="filter-group">
            <input
                type="text"
                data-search
                placeholder="搜索宝石名称..."
                class="gufeng-song gem-search"
            />
        </div>
    </div>

    <div class="not-content grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3">
        {gems.map((gem: any) => <GemCard gem={gem} />)}
    </div>

    <div class="gem-empty" data-empty-state>
        <p class="gufeng-kai">未找到符合条件的宝石</p>
    </div>
</div>

<script>
    (function () {
        const container = document.querySelector("[data-gem-showcase]");
        if (!container) return;
        const searchInput = container.querySelector("[data-search]");
        const items = container.querySelectorAll("[data-item]");
        const emptyState = container.querySelector("[data-empty-state]") as HTMLElement | null;

        function filter() {
            const query = (searchInput as HTMLInputElement)?.value.toLowerCase().trim() || "";
            let visibleCount = 0;
            items.forEach((item) => {
                const name = (item.getAttribute("data-name") || "").toLowerCase();
                const visible = !query || name.includes(query);
                (item as HTMLElement).style.display = visible ? "" : "none";
                if (visible) visibleCount++;
            });
            if (emptyState) {
                emptyState.style.display = visibleCount === 0 ? "" : "none";
            }
        }

        if (searchInput) searchInput.addEventListener("input", filter);
    })();
</script>

<style>
    .gem-filters {
        display: flex;
        flex-direction: column;
        flex-wrap: wrap;
        gap: 0.75rem;
        margin-bottom: 1.5rem;
        padding: 1rem;
        background: var(--card);
        border: 1px solid var(--border);
        position: relative;
    }
    @media (min-width: 768px) {
        .gem-filters {
            flex-direction: row;
            align-items: center;
        }
    }
    .gem-filters::before {
        content: "";
        position: absolute;
        inset: 5px;
        border: 1px solid var(--border);
        pointer-events: none;
        opacity: 0.4;
    }
    .filter-group {
        display: flex;
        align-items: center;
        gap: 0.625rem;
        flex-wrap: wrap;
        position: relative;
        z-index: 1;
    }
    .gem-search {
        width: 100%;
        max-width: 24rem;
        padding: 0.5rem 0.75rem;
        border: 1px solid var(--border);
        background: var(--background);
        color: var(--foreground);
        font-size: 0.875rem;
        outline: none;
        transition: border-color 0.2s ease;
    }
    .gem-search:focus {
        border-color: var(--primary);
    }
    .gem-empty {
        display: none;
        text-align: center;
        padding: 3rem 1rem;
        color: var(--muted-foreground);
        font-size: 1rem;
    }
</style>
